<h1><%= t('.tests') %></h1>

<table class="table table-striped">
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.level') %></th>
    <th  colspan = "3"><%= t('.actions') %></th>
  </tr>

  <%= render @tests %>
</table>

<%= link_to t('.new_test'), new_admin_test_path %>
<%= link_to 'All Gist', admin_gists_path %>
