<p>
  <strong>Body:</strong>
  <%= @question.body %>
<p>

<%= link_to 'Back', test_path(@question.test) %>
<%= link_to 'Edit',  edit_question_path %>
<%= link_to 'Delete question', question_path(@question), method: :delete, data: { confirm: "Are you sure to delete?" } %>